import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Configuração para Vercel (suporta API Routes)
  // Remover 'output: export' para habilitar Server-Side Features
};

export default nextConfig;
