# =============================================================================
# VTEX Budget Audit - Variáveis de Ambiente
# =============================================================================
# Copie este arquivo para .env.local e preencha com seus valores

# -----------------------------------------------------------------------------
# Configurações da conta VTEX
# -----------------------------------------------------------------------------
VTEX_ACCOUNT=myaccount
VTEX_ENVIRONMENT=vtexcommercestable

# -----------------------------------------------------------------------------
# Credenciais de autenticação VTEX
# Obtenha em: Admin VTEX > Account Settings > Application Keys
# -----------------------------------------------------------------------------
VTEX_APP_KEY=vtexappkey-xxx-xxx
VTEX_APP_TOKEN=your-app-token-here

# -----------------------------------------------------------------------------
# Master Data
# -----------------------------------------------------------------------------
# Nome da entidade no Master Data que armazena os orçamentos
MASTERDATA_ENTITY=budget

# -----------------------------------------------------------------------------
# Configurações de comparação
# -----------------------------------------------------------------------------
# Threshold percentual para marcar diferença como crítica (ex: 0.5 = 0.5%)
CRITICAL_DIFF_THRESHOLD_PCT=0.5

# Threshold absoluto em reais para marcar diferença como crítica
CRITICAL_DIFF_THRESHOLD_ABS=50

# -----------------------------------------------------------------------------
# Opcional - Host público para chamadas client-side (se necessário)
# -----------------------------------------------------------------------------
# NEXT_PUBLIC_VTEX_STORE_HOST=mystore.vtexcommercestable.com.br

# -----------------------------------------------------------------------------
# Autenticação (Auth.js / NextAuth)
# -----------------------------------------------------------------------------
# Secret para JWT (gerar com: openssl rand -base64 32)
NEXTAUTH_SECRET=your-secret-here

# URL do deploy (ex: https://app.vercel.app)
NEXTAUTH_URL=http://localhost:3000

# -----------------------------------------------------------------------------
# Banco de dados (Prisma)
# -----------------------------------------------------------------------------
# URL de conexão com Postgres (Prisma Postgres, Neon, Supabase, etc.)
BU_PRISMA_DATABASE_URL=prisma+postgres://accelerate.prisma-data.net/?api_key=your-api-key
